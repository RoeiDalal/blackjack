build_image:
  image: docker
  before_script:
    - docker login registry.gitlab.com -u $REGISTRY_USER -p $REGISTRY_PASS
  script:
    - docker build -t registry.gitlab.com/roeidalal/blackjack .
    - docker push registry.gitlab.com/roeidalal/blackjack
